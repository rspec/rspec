name: RSpec CI

on:
  push:
    branches:
      - 'main'
      - '*-maintenance'
  pull_request:
    branches:
      - '*'

permissions:
  contents: read

jobs:
  docs:
    uses: benoittgt/rspec/.github/workflows/documentation_coverage.yml@ruby-3-4

  rubocop:
    uses: benoittgt/rspec/.github/workflows/rubocop.yml@ruby-3-4

  core:
    uses: benoittgt/rspec/.github/workflows/rspec.yml@ruby-3-4
    with:
      library: 'rspec-core'
      rspec_version: '= 3.14.0.pre'

  mocks:
    uses: benoittgt/rspec/.github/workflows/rspec.yml@ruby-3-4
    with:
      library: 'rspec-mocks'
      rspec_version: '= 3.14.0.pre'

  expectations:
    uses: benoittgt/rspec/.github/workflows/rspec.yml@ruby-3-4
    with:
      library: 'rspec-expectations'
      rspec_version: '= 3.14.0.pre'

  support:
    uses: benoittgt/rspec/.github/workflows/rspec.yml@ruby-3-4
    with:
      library: 'rspec-support'
      rspec_version: '= 3.14.0.pre'
