name: RSpec CI

on:
  push:
    branches:
      - 'main'
      - '*-maintenance'
  pull_request:
    branches:
      - '*'

permissions:
  contents: read

jobs:
  docs:
    uses: rspec/rspec/.github/workflows/documentation_coverage.yml@ruby-4

  rubocop:
    uses: rspec/rspec/.github/workflows/rubocop.yml@ruby-4

  core:
    uses: rspec/rspec/.github/workflows/rspec.yml@ruby-4
    with:
      library: 'rspec-core'
      rspec_version: '= 4.0.0.pre'

  mocks:
    uses: rspec/rspec/.github/workflows/rspec.yml@ruby-4
    with:
      library: 'rspec-mocks'
      rspec_version: '= 4.0.0.pre'

  expectations:
    uses: rspec/rspec/.github/workflows/rspec.yml@ruby-4
    with:
      library: 'rspec-expectations'
      rspec_version: '= 4.0.0.pre'

  support:
    uses: rspec/rspec/.github/workflows/rspec.yml@ruby-4
    with:
      library: 'rspec-support'
      rspec_version: '= 4.0.0.pre'
